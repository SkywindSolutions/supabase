# Grafana dashboard provisioning — Skywind Infrastructure
# Auto-generated by generate_group_dashboards.py
# DO NOT EDIT BY HAND — re-run the script after adding new groups.
#
# Two base providers (Internal / Customer reference) plus one provider
# per customer group, each isolated in its own Grafana folder.
#
# disableDeletion: false — dashboard is removed when the JSON file is
#                          deleted (keeps provisioned folders in sync).
# editable: false        — changes made in the UI are not saved to disk.

apiVersion: 1

providers:
  # ----------------------------------------------------------------
  # Internal dashboards — Skywind staff only
  # ----------------------------------------------------------------
  - name: Internal Dashboards
    orgId: 1
    folder: Internal Dashboards
    folderUid: internal-dashboards
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/internal

  # ----------------------------------------------------------------
  # Shared customer templates — visible to Internal team only.
  # Customers are routed to their group-specific folder below.
  # ----------------------------------------------------------------
  - name: Customer Dashboards
    orgId: 1
    folder: Customer Dashboards
    folderUid: customer-dashboards
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/customer

  # ----------------------------------------------------------------
  # Per-customer-group folders
  # Each folder is restricted to one Grafana team via API permissions
  # (see scripts/provision_grafana_access.sh).
  # Only dashboards for data types the group actually has are written
  # to the group's subdirectory, so empty-data dashboards are never
  # provisioned and will not appear in the Grafana folder.
  # ----------------------------------------------------------------

  # Chatham Forecasts (tide)
  - name: Chatham Forecasts
    orgId: 1
    folder: Chatham Forecasts
    folderUid: cust-grp-chatham
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/groups/grp_chatham

  # Clearwater Forecasts (tide)
  - name: Clearwater Forecasts
    orgId: 1
    folder: Clearwater Forecasts
    folderUid: cust-grp-clearwater
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/groups/grp_clearwater

  # Corpus Christi Forecasts (visibility)
  - name: Corpus Christi Forecasts
    orgId: 1
    folder: Corpus Christi Forecasts
    folderUid: cust-grp-corpuschristi
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/groups/grp_corpuschristi

  # Mobile Bay Forecasts (visibility, wind, wind_visibility)
  - name: Mobile Bay Forecasts
    orgId: 1
    folder: Mobile Bay Forecasts
    folderUid: cust-grp-mobilebay
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/groups/grp_mobilebay

  # Ocean Cay Forecasts (wind)
  - name: Ocean Cay Forecasts
    orgId: 1
    folder: Ocean Cay Forecasts
    folderUid: cust-grp-oceanCay
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/groups/grp_oceanCay

  # Pascagoula Forecasts (visibility, wind, wind_visibility)
  - name: Pascagoula Forecasts
    orgId: 1
    folder: Pascagoula Forecasts
    folderUid: cust-grp-pascagoula
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/groups/grp_pascagoula

  # Port Richey Forecasts (tide)
  - name: Port Richey Forecasts
    orgId: 1
    folder: Port Richey Forecasts
    folderUid: cust-grp-portrichey
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/groups/grp_portrichey

  # Sendero Forecasts (wind)
  - name: Sendero Forecasts
    orgId: 1
    folder: Sendero Forecasts
    folderUid: cust-grp-sendero
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/groups/grp_sendero

  # SSA Marine Forecasts (tide, tide_wind, wind)
  - name: SSA Marine Forecasts
    orgId: 1
    folder: SSA Marine Forecasts
    folderUid: cust-grp-ssamarine
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/groups/grp_ssamarine
